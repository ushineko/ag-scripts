## Validation Report: v2.0 — macOS Support

**Date**: 2026-06-23
**Spec**: specs/001-macos-support.md
**Status**: PASSED

### Phase 3: Tests
- Syntax: `bash -n` on `gather.sh`, `install.sh`, `uninstall.sh`, `test_applescript_gather.sh` — all clean
- Functional smoke tests on macOS (Darwin, Built-in Retina Display):
  - `./gather.sh --version` → `vscode-gather v2.0`
  - `./gather.sh --list-outputs` → `1. Built-in Retina Display (PRIMARY) — 2056x1329` (exercises NSScreen/JXA display detection)
  - `./gather.sh --help` → exit 0
  - `./gather.sh --dry-run` → prints correct AppleScript with menu-bar-adjusted bounds (`position {0, 39}`, `size {2056, 1290}` = visibleFrame), no Accessibility required
- `test_applescript_gather.sh` is a manual macOS harness (Tests 1–5) requiring a running VS Code + Accessibility grant; not run in this non-interactive context (documented as manual).
- Status: PASSED (automated paths verified; window-move requires manual run with Accessibility)

### Phase 4: Code Quality
- Single `gather.sh` entry point with `uname -s` platform branching; Linux KWin path untouched (no regression)
- Symmetric `_linux` / `_macos` helpers for `detect_primary_output`; macOS window logic isolated in `get_display_bounds` / `generate_applescript` / `gather_macos`
- Fix during finalization: reordered `gather_macos` so `--dry-run` prints without requiring Accessibility (bounds come from NSScreen, which doesn't need it)
- Doc fix: corrected `--output` help text (removed unimplemented "display index" claim; macOS matches display name only)
- Status: PASSED

### Phase 5: Security Review
- `shellcheck` **not installed** on this host — static lint skipped (not auto-installed per project tool policy); `bash -n` clean on all scripts.
- No hardcoded secrets. No `eval`. `osascript`/`python3` invoked with heredoc'd scripts; the only interpolated values are integer display bounds (parsed to `int` via `python3 json`) and the process/output names from the user's own `--class`/`--output` flags.
- `install.sh` writes a symlink into `/usr/local/bin` (macOS) / `~/bin` (Linux); no privileged operations beyond `mkdir -p`.
- Accessibility is a hard macOS gate; the script detects denial and exits with guidance rather than failing silently.
- Status: PASSED (shellcheck deferred — tool unavailable)

### Phase 5.5: Release Safety
- Change type: Additive macOS support (new platform branches) + two small finalization fixes. Linux/KDE behavior unchanged.
- Rollback plan: `git revert` the commit. On a user's machine, `./uninstall.sh` removes the symlink.
- Status: PASSED

### Overall
- All gates passed: YES (shellcheck deferred — tool unavailable; `bash -n` clean)
- Notes: macOS window manipulation (the `--move` path) requires a running VS Code and an Accessibility grant, so it must be verified manually on the target machine; all dependency-free paths (version, help, list-outputs, dry-run, display detection) are verified working on macOS.
