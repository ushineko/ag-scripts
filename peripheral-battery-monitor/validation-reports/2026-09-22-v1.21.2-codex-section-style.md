# Validation Report — v1.21.2 Codex Section Style Parity

**Date**: 2026-09-22
**Spec**: `specs/042-codex-section-style.md`
**Issue**: [#19](https://github.com/ushineko/ag-scripts/issues/19)

## Result

The Codex section now participates in the peripheral monitor's shared style and
scaled layout. It has the same translucent frame, border, radius, icon,
typography, margins, header spacing, refresh button, and progress colors as the
Claude section.

## Validation

- Peripheral suite: 278 passed, 36 subtests passed.
- A real offscreen Qt render confirms the inherited frame paints an opaque
  `#232323` background when monitor opacity is 100%.
- Focused Ruff, compileall, and `git diff --check`: clean.
- Manual live verification follows deployment and process restart.

## Security

No network, credential, subprocess, dependency, or configuration behavior
changed. The diff contains no secrets. The existing `pip-audit` installation
remains unusable because its environment lacks `filelock`; dependency files
are unchanged.

## Release safety

Presentation-only. Codex fetching, caching, settings, and normalized data are
unchanged. Rollback is a normal commit revert.
