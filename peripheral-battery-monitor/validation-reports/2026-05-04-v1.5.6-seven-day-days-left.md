## Validation Report: v1.5.6 Seven-Day Quota Days-Left Indicator
**Date**: 2026-05-04
**Commit**: (pending)
**Status**: PASSED

### Phase 3: Tests
- Test suite: `python3 -m pytest tests/ -x -q`
- Results: 36 passing, 0 failing
- New tests: 3 (`test_get_days_until_reset_multi_day`, `test_get_days_until_reset_sub_day`, `test_get_days_until_reset_past_or_invalid`)
- Updated test: `test_update_claude_section_with_api_data` now uses a future `seven_day.resets_at` and asserts `"7d: 25% (5d left)"` to exercise the new branch
- Status: PASSED

### Phase 4: Code Quality
- Dead code: None introduced
- Duplication: `get_days_until_reset` mirrors structure of `get_time_until_reset`; both kept separate because output format and rounding rules differ
- Encapsulation: Helper is module-level alongside the existing reset-time helper; render code is one local var + `if days_left` append
- Refactorings: None needed
- Status: PASSED

### Phase 5: Security Review
- Dependencies (tool-verified): No new dependencies added
- OWASP Top 10 (AI-assisted, best-effort): No new attack surfaces. Input is the same `resets_at` ISO string already consumed by `get_time_until_reset`; `datetime.fromisoformat` exceptions are caught
- Anti-patterns (AI-assisted, best-effort): No hardcoded secrets, no injection vectors, no logging of sensitive data
- Fixes applied: None needed
- Note: AI-assisted findings are a developer aid, not compliance evidence
- Status: PASSED

### Phase 5.5: Release Safety
- Change type: UI-only label augmentation; additive
- Rollback plan: `git revert` — no schema, config, or persisted state changes
- Status: PASSED

### Overall
- All gates passed: YES
- Notes: Bottom-right weekly quota label now reads `7d: NN% (Md left)` when `seven_day.resets_at` is a future timestamp. Sub-day shows `<1d left`. Past/missing/invalid timestamps render the original `7d: NN%` form, preserving backward compatibility for cached data without `resets_at`.
