# Validation Report — v1.21.1 Codex Desktop-Path Discovery

**Date**: 2026-09-22
**Spec**: `specs/041-codex-desktop-path.md`
**Issue**: [#17](https://github.com/ushineko/ag-scripts/issues/17)

## Result

The desktop-launched peripheral monitor can now find the Miniforge-installed
Codex CLI despite its restricted PATH and therefore creates the Codex section.

## Validation

- Peripheral suite: 277 passed, 36 subtests passed.
- Restricted-PATH live smoke: resolved `~/miniforge3/bin/codex` and completed
  an app-server usage read.
- Ruff and `git diff --check`: clean.
- Mirrored provider modules: byte-identical.

## Security

The resolved executable is launched as a fixed argument vector without a
shell. No credential, dependency, or agent-configuration changes were made.

## Release safety

The change affects discovery only and requires no settings migration. Existing
launchers remain valid. Rollback is a normal commit revert.
