# The markdown previewer (glow.yazi) is vendored under plugins/ instead of being
# managed here: upstream Reledia/glow is unmaintained and broke on yazi 26.x
# (removed Command:args, ya.mgr_emit, ya.preview_widgets, ui.Text.parse). The
# local fork in plugins/glow.yazi/main.lua is ported to the current API. Keeping
# it out of `ya pkg` avoids `ya pkg upgrade` clobbering the fork with the broken
# upstream. See plugins/glow.yazi/main.lua.

[[flavor.deps]]
use = "BennyOe/tokyo-night"
rev = "8e6296f"
hash = "49258e379077f4b0d2e62dfdb131c7d"
