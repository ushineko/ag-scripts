[project]
name = "slack-presence-toggle"
version = "0.1.1"
description = "Tray utility that toggles Slack presence based on Slack window focus on KDE Plasma 6 / Wayland."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
