## Validation Report: Gamescope Launcher (v2.0.0)
**Date**: 2026-04-25
**Status**: PASSED
**Spec**: `specs/003-gamescope-launcher.md`

### Phase 3: Tests
- Test suite: `pytest tests/ -v`
- Results: 20 passing, 0 failing (`test_launcher.py` 13, `test_install_kwin_rule.py` 7)
- Coverage: Heroic config parsing (happy path, missing file, missing app id, proton rejection, missing fields), portrait monitor auto-detection (single, multiple, none, empty, explicit override, explicit unknown), env construction, gamescope command construction, KWin rule install (geometry, idempotency, preserves unrelated, legacy migration), KWin rule uninstall (current+legacy, no-op, preserves unrelated)
- Status: PASSED

### Phase 4: Code Quality
- Dead code: None found (`pyflakes` clean)
- Duplication: None found
- Encapsulation: `pinball_fx_launch.py` and `install_kwin_rule.py` separated by concern, all functions under 30 lines, KWin rule logic follows the established `foghorn-leghorn` / `peripheral-battery-monitor` pattern
- Status: PASSED

### Phase 5: Security Review

**Tooling**:
- `pip-audit --no-deps` against `PyQt6`: no known vulnerabilities (pip-audit 2.x)
- `pyflakes` against all Python sources: clean
- `shellcheck` against `install.sh`, `uninstall.sh`: clean

**Secrets check**: `grep -rEn "(password|secret|token|api[-_]?key|BEGIN [A-Z]+ PRIVATE KEY)"` — no hardcoded credentials in any source file (only spec text containing the word "secrets" as part of "secrets check").

**OWASP review (changed code only)**:
- **A01 Broken Access Control**: All file writes restricted to `~/.config/kwinrulesrc` (default) or a caller-supplied path. Caller-supplied path is only used in tests. No path traversal surface.
- **A03 Injection**: All `subprocess.run` calls use list form, no `shell=True`. Wine/gamescope command is built from `argparse`-validated paths and integer screen geometry. `os.execvpe` invocation mirrors `subprocess` semantics — no shell.
- **A05 Security Misconfiguration**: KWin rule additions only — does not expose new network surfaces, ports, or authentication paths.
- **A06 Vulnerable Components**: Only third-party dep is PyQt6, version-clean per pip-audit.
- **A08 Integrity**: No code download/exec from network. Heroic config is JSON-parsed via stdlib.
- **A09 Logging Failures**: Launcher logs the resolved command + screen to stdout. No credentials or sensitive paths beyond user-owned game install paths.
- Other categories (A02 Cryptographic Failures, A04 Insecure Design, A07 Auth Failures, A10 SSRF): not applicable to a desktop launcher.

**Limitations**: This is an AI-assisted review of changed code, not a compliance artifact. Manual review still recommended for changes touching trust boundaries.

- Status: PASSED

### Phase 5.5: Release Safety
- **Change type**: Code-only (new launcher, new KWin rule installer, replaces old fixer).
- **Reversibility**: `./uninstall.sh` removes the desktop entry and the KWin rule (both current and legacy v1.x). To revert to v1.x, `git checkout <prior-commit>` and re-run `install.sh`.
- **Backward compat**: Installer drops the v1.x `PinballFixer.desktop` and the v1.x KWin rule on install. Uninstaller does the same defensively. No user data touched.
- **Rollback plan documented**: Yes (in this report).
- Status: PASSED

### Phase 6.5: Spec Reconciliation
All 14 acceptance criteria in `specs/003-gamescope-launcher.md` verified and checked. Spec status updated to COMPLETE.

### Smoke Tests
- `./pinball_fx_launch.py --version` → `pinball-fx-launch 2.0.0`
- `./pinball_fx_launch.py --help` → argparse output as expected
- `./pinball_fx_launch.py --dry-run` → printed `gamescope -W 1440 -H 2560 -w 1440 -h 2560 -f -- /usr/bin/wine /home/nverenin/Games/Heroic/PinballFX/PinballFX.exe`, screen 0 (portrait, KDE 1.5x scaled logical coords). KWin rule was NOT touched in dry-run mode (deferred until real launch).

### Changes in v2.0.0
- `pinball_fx_launch.py` — New gamescope-wrapping launcher.
- `install_kwin_rule.py` — New KWin rule installer for `wmclass=gamescope` placement; also migrates/removes the v1.x `Pinball FX Portrait Mode` rule.
- `PinballFX.desktop` — Replaces `PinballFixer.desktop`. `Categories=Game;`.
- `install.sh`, `uninstall.sh` — Rewritten to set up / tear down the new launcher + clean up v1.x state.
- `tests/` — New pytest suite (`test_launcher.py`, `test_install_kwin_rule.py`, `conftest.py`).
- `README.md` — Rewritten for v2.0.0; changelog updated.
- `specs/003-gamescope-launcher.md` — New spec, marked COMPLETE.
- `configure_kwin.py`, `PinballFixer.desktop` — Deleted.
- Root `README.md` — `pinball-fx` row updated.

### Overall
- All gates passed: YES
