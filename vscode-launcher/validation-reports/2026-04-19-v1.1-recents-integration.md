## Validation Report: VSCode Recents Integration (v1.1)
**Date**: 2026-04-19
**Spec**: 002-recents-integration
**Status**: PASSED

### Phase 3: Tests
- Test suite: `/usr/bin/python3 -m pytest tests/ -v`
- Results: 33 passing, 0 failing (was 24 in v1.0; 9 new tests cover URI parsing, label conventions, recents reader edge cases, v1→v2 migration, and main-window integration with mocked recents)
- Contract focus: the new tests verify the *format contract* of VSCode's state DB (folderUri / workspace.configPath shape, file:// URI decoding, non-file scheme skip). If VSCode changes the stored JSON format in a future version, these tests will fail early
- Live smoke: a standalone import test parsed the user's real `state.vscdb` and returned 12 entries matching VSCode's own Recent list
- Status: PASSED

### Phase 4: Code Quality
- Dead code: removed along with the refactor (Workspace.id, Workspace.to_dict/from_dict, ConfigManager.parse_workspaces, WorkspaceListWidget.iter_workspace_ids, AddEditDialog, Add/Edit/Remove toolbar actions)
- Duplication: none
- Encapsulation: VSCodeRecentsReader isolates all SQLite/JSON parsing; ConfigManager's migration path is a clearly-named private method
- Overcomplication: considered a "sync-once-from-VSCode" model but rejected — dynamic reads are strictly simpler (no state to re-sync when VSCode opens a new workspace)
- Status: PASSED

### Phase 5: Security Review
- New surface: SQLite read of `~/.config/Code/User/globalStorage/state.vscdb`
  - Opened read-only (`mode=ro`, `uri=True`) so the launcher cannot mutate VSCode's state even by accident
  - Query uses a parameterized statement (`SELECT value FROM ItemTable WHERE key = ?`)
  - JSON parsed with `json.loads` (no pickle, no eval)
  - Non-`file://` URIs dropped, defending against `javascript:` or other exotic schemes ending up in paths passed to subprocess
- All previous invariants preserved: subprocess calls remain list-form; no `shell=True`; no `eval`/`exec`; no secrets
- AST scan confirmed (manual review + prior `shell=True`/`eval`/`exec` scan still clean)
- Status: PASSED

### Phase 5.5: Release Safety
- Change type: feature refactor (data source pivot + config schema v2)
- Rollback plan: `git revert` restores v1.0 code; the v1→v2 config migration is one-way, so users who revert after running v1.1 will need to re-add workspaces manually. Since v1.0 was only installed briefly and no real user data is on disk in this session, this is not load-bearing.
- Backward compatibility: v1 config files are detected and migrated; `tmux_session` mappings are preserved
- Behavior visible to users: toolbar changed (Add/Edit/Remove gone; Refresh/Set Tmux Session/Hide/Unhide All added). Documented in the README changelog.
- Status: PASSED

### Phase 6.5: Spec Reconciliation
- Spec 001 marked as superseded-in-part (manual list → VSCode recents); Tmux plumbing and window placement unchanged
- Spec 002 created with 14 acceptance criteria, all checked; status COMPLETE
- Both specs committed with the implementation
- Status: PASSED

### Files Changed / Added
- **Modified**: `vscode_launcher.py` (~40% rewritten — new reader, v2 config, new UI actions)
- **Modified**: `tests/test_unit_vscode_launcher.py` (expanded to 33 tests, 9 new)
- **Modified**: `README.md` (new Changelog entry for v1.1, new Usage sections for Set Tmux Session / Hide / Unhide All / Refresh)
- **Modified**: `specs/001-vscode-launcher.md` (marked superseded-in-part)
- **New**: `specs/002-recents-integration.md`
- **Modified**: root `README.md` (updated project description)

### Overall
- All gates passed: YES
- Version bumped in source: 1.0 → 1.1 (pending user confirmation at commit time)
