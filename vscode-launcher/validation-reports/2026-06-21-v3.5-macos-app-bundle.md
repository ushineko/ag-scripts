## Validation Report: v3.5 macOS .app bundle (spec 014)
**Date**: 2026-06-21
**Commit**: pre-commit
**Status**: PASSED

### Scope
Packages vscode-launcher as a native macOS `.app` (PyInstaller), resolving the
contradiction in spec 013 (which said `.app` was out of scope while directing
"follow clockwork-orange's pattern" — clockwork-orange ships a PyInstaller
`.app`). Adds frozen-aware resource loading, an `.icns` generator, a build
script, and `.app`-aware install/uninstall. Supersedes the `.app` out-of-scope
line in spec 013.

### Phase 3: Tests
- Command: `QT_QPA_PLATFORM=offscreen python3 -m pytest -q`
- Results: **168 passing, 0 failing** (unchanged; no test logic affected by
  the `_resource_dir` refactor).
- Status: PASSED

### Runtime verification (macOS 26.5, M1 Max, VS Code 1.125.1)
- **Build**: `scripts/build_macos.sh` produced `dist/vscode-launcher.app` on
  system python3 3.9.6 (framework build). `.icns` generated (1024px `ic12`).
- **Frozen smoke test**: ran the bundle binary `--tray` for 5s — event loop
  alive, KGlobalAccel degraded gracefully, no crash. MainWindow construction
  (which reads recents) completed without exception.
- **Info.plist**: `LSUIElement = true`, `CFBundleIdentifier = com.vscode-launcher`.
- **Live install**: `./install.sh` copied the `.app` to `/Applications`,
  symlinked `vscode-launcher` → the bundle binary, pointed the LaunchAgent at
  it, and `launchctl bootstrap` started it (running pid confirmed from the
  bundle path).
- **Spotlight**: `mdfind` returns `/Applications/vscode-launcher.app`.
- **Live uninstall**: removed the `.app`, both symlinks, the LaunchAgent + job,
  the running process, and the hook block.
- **zsh round-trip**: install → uninstall left `~/.zshrc` byte-identical to the
  pre-install backup (`diff` clean) after the blank-line-eating awk fix.
  Machine left with the app installed at the user's request.

### Phase 4: Code Quality
- Dead code: none. `_resource_dir()` is used by `_resolve_app_icon`.
- Duplication: none introduced. The `.app` install/uninstall reuse the
  existing platform-branch structure.
- Encapsulation: resource-path logic isolated in one helper; build logic lives
  in `scripts/`; install logic stays in the macOS branch of install.sh.
- Status: PASSED

### Phase 5: Security Review
- Dependency scanners (`pip-audit`/`trufflehog`/`detect-secrets`/`git-secrets`):
  not installed; not auto-installed per project policy. New build dependency is
  `pyinstaller` (`pip3 install --user`, build-time only, user-approved).
- OWASP (AI-assisted, best-effort): clean. New surface is a PyInstaller spec
  (script-controlled config), an SVG→`.icns` renderer invoking `iconutil`, and
  shell install steps writing a plist from script-constructed paths — no user
  input crosses a trust boundary, no network/injection/deserialization.
- Secrets scan (inline): 0 findings (one "token" hit is a code comment).
- Code signing: the `.app` is adhoc-signed (PyInstaller default). Developer ID
  signing / notarization is explicitly out of scope (spec 014).
- Status: PASSED

### Phase 5.5: Release Safety
- Change type: additive macOS packaging; Linux install path unchanged (still
  symlinks the source script).
- Rollback: `git revert <commit>`; the shipped `uninstall.sh` removes the `.app`
  and all artifacts (verified live). Build outputs are gitignored.
- Status: PASSED

### Overall
- All gates passed: YES
- Spec 014 acceptance criteria: all checked; status COMPLETE. Out-of-scope:
  code signing/notarization, `.dmg`/Releases distribution, Windows, Homebrew cask.
