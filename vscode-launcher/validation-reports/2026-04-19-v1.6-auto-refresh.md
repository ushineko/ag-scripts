## Validation Report: Background Auto-Refresh of Running State (v1.6)
**Date**: 2026-04-19
**Spec**: 007-auto-refresh-running-state
**Status**: PASSED

### Phase 3: Tests
- Test suite: `/usr/bin/python3 -m pytest tests/ -q`
- Results: 88 passing, 0 failing (was 85 in v1.5; 3 new tests)
- New coverage:
  - `test_auto_refresh_updates_running_state_in_place_no_sort` — 3-row table with two rows transitioning from idle → running via `_on_background_scan_done`; asserts row order unchanged, flags flipped, Actions and Checkbox cells rebuilt correctly (Start → [Activate][Stop], enabled → disabled)
  - `test_auto_refresh_callback_skips_when_scanner_returns_none` — a None result (KWin / journalctl unavailable during the poll) must not clobber existing state
  - `test_trigger_background_scan_skips_when_hidden` — visibility gate: a MainWindow that hasn't been `show()`n has `isVisible()` False, so `_trigger_background_scan` short-circuits and never calls the scanner
- Status: PASSED

### Phase 4: Code Quality
- Dead code: none
- Duplication: none — the worker pattern is isolated to `_ScanWorker`; the in-place cell rebuild reuses the `_build_*_cell` helpers already factored out in v1.5
- Encapsulation: `_ScanWorker` is a minimal QObject; `update_row_running_state` lives on the widget that owns the cells
- Thread safety:
  - The scanner's existing reentrance protection (fixed script name in KWin) is layered against the `_trigger_background_scan` reentrance guard — two independent checks
  - Signals cross thread boundaries correctly (Qt auto-marshals `finished` to the receiver's thread)
  - `deleteLater` on both worker and thread ensures clean teardown
- Refactorings: none needed
- Status: PASSED

### Phase 5: Security Review
- New surface: a worker thread executes the same `WindowScanner.list_vscode_captions` path validated in specs 004 and 005 — no new subprocess, no new file I/O, no new env-var handling
- `_ScanWorker.run` wraps the scan in a broad `try/except Exception` so worker failures can't propagate to the main thread. Worst case: a `None` result and the next poll retries
- No new data persisted; running state is runtime-only (reverted on every startup)
- No new user-input surface
- Status: PASSED

### Phase 5.5: Release Safety
- Change type: additive feature (new auto-refresh behavior)
- Rollback plan: `git revert HEAD` restores v1.5 (manual Refresh only). No schema or config changes; no filesystem state to clean up.
- Backward compatibility: full — the feature activates automatically when a `WindowScanner` is configured and degrades silently when KWin or journalctl are unavailable
- Resource overhead: ~500 ms scan every 5 s while the launcher window is visible (~10% duty cycle). Drops to zero when minimized. Document in README for transparency.
- No new install / uninstall changes needed — ships purely as code inside `vscode_launcher.py`
- Status: PASSED

### Phase 6.5: Spec Reconciliation
- Spec 007 created; 13 acceptance criteria checked; status COMPLETE
- README updated with v1.6 changelog entry and a rewritten Refreshing section clarifying the auto-refresh-vs-manual-Refresh split
- All files committed together in the upcoming v1.6 commit
- Status: PASSED

### Files Added
- `specs/007-auto-refresh-running-state.md`
- `validation-reports/2026-04-19-v1.6-auto-refresh.md` (this report)

### Files Modified
- `vscode_launcher.py`:
  - `QObject` and `QThread` added to `PyQt6.QtCore` imports
  - `AUTO_REFRESH_INTERVAL_MS = 5000` constant
  - New `_ScanWorker(QObject)` class (10 lines): wraps `list_vscode_captions` in a worker thread, emits `finished(list | None)`
  - `WorkspaceTableWidget.update_row_running_state(row, workspace)` — rebuilds Checkbox / Status / Actions cells in place
  - `MainWindow.__init__` tracks `_scan_thread` and `_auto_refresh_timer`
  - `MainWindow._start_auto_refresh` — QTimer wiring
  - `MainWindow._trigger_background_scan` — visibility gate + reentrance guard + thread launch
  - `MainWindow._on_background_scan_done` — UI-thread callback that updates is_running in place (no re-sort)
  - `MainWindow.closeEvent` — stops timer, waits on in-flight thread with a 2 s bound before saving config
  - `__version__` bumped to 1.6
- `tests/test_unit_vscode_launcher.py` — 3 new tests
- `README.md` — v1.6 changelog entry + rewritten Refreshing section

### Overall
- All gates passed: YES
- Version: 1.5 → 1.6
