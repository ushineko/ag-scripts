## Validation Report: PWD-Based Tmux Lookup (v1.2)
**Date**: 2026-04-19
**Spec**: 003-pwd-based-tmux-lookup
**Status**: PASSED

### Phase 3: Tests
- Test suite: `/usr/bin/python3 -m pytest tests/ -q`
- Results: 47 passing, 0 failing (was 33 in v1.1; 15 new `tmux_lookup` tests, 1 removed env-var test suite no longer applicable)
- New coverage: parent walk, longest-match precedence, sibling-prefix rejection (`/a/bcd` vs `/a/b`), `.code-workspace` folder resolution (relative + absolute paths, malformed entries, missing files, bad JSON)
- Live smoke: `vscl-tmux-lookup` was run against the real user config for multiple PWDs — `ag-scripts` (direct folder), `ag-scripts/subdir` (parent walk), `aiq_agent_go` (resolved from `aiq_agent_go.code-workspace` with `../git/aiq_agent_go` relative path), and an unmapped path (exit 1)
- End-to-end verified by the user: opening a fresh integrated terminal in a mapped VSCode window now attaches/switches to the correct tmux session for both folder and `.code-workspace` entries
- Status: PASSED

### Phase 4: Code Quality
- Dead code: removed `build_launch_env` helper and all env-var plumbing from `Launcher.launch_workspace` (no longer needed after pivot to PWD-based lookup)
- `os` import removed from `vscode_launcher.py` (env var code was its only user)
- Duplication: none
- Encapsulation: `tmux_lookup.py` is a single-purpose helper, ~90 lines, three pure functions plus a main
- Comment hygiene: added a header in `tmux_hook.zsh` explaining WHY placement matters (tmux strips VSCODE_* env vars and rewrites TERM_PROGRAM) — this is a non-obvious invariant worth calling out
- Refactorings: none beyond the pivot itself
- Status: PASSED

### Phase 5: Security Review
- New surfaces:
  - `tmux_lookup.py` reads `~/.config/vscode-launcher/workspaces.json` and arbitrary `.code-workspace` files listed in that config. All files read via standard library `Path.read_text()` and `json.loads` — no `pickle`, no `eval`
  - The helper prints the session name as the process's only stdout. The session name originates from the user's own config, so no untrusted-data-to-shell path
  - Exit code is the only signal consumed by the shell hook; `printf "$session"` is not executed, we use command substitution then pass as `-t` arg to `tmux` (list-form, not shell interpolation)
- Catch-all `try/except Exception` in `tmux_lookup.main` is intentional (runs inside shell startup — a stack trace would break the user's shell). All exceptions lead to exit 1 with no output.
- `is_under` uses `os.sep`-suffix check to reject sibling-prefix false positives — important security/correctness property covered by a dedicated test (`test_prefix_but_not_child_rejected`, `test_sibling_not_matched`)
- No new subprocess calls beyond what was already reviewed in v1.0/v1.1
- Secrets scan: clean (no changes to files with any credential-like content)
- Status: PASSED

### Phase 5.5: Release Safety
- Change type: behavior fix + small-scale refactor
- Rollback plan: `./uninstall.sh` removes all symlinks (including `vscl-tmux-lookup`), desktop entry, and the zsh hook block (with automatic backup at `~/.zshrc.vscode-launcher.bak`)
- `install.sh` now replaces the hook block in place when reinstalling, so users get the latest hook body without moving it
- Fresh installs on zshrc with an existing tmux auto-attach: `install.sh` detects the first `tmux new-session|attach|a` or `initialize_tmux` line and inserts the hook BEFORE it; falls back to end-of-file + a warning comment if no such line is found
- User's zshrc manually repositioned during this session (hook moved from EOF to before `initialize_tmux` at line 258); future reinstalls preserve that position via the in-place replacement path
- Status: PASSED

### Phase 6.5: Spec Reconciliation
- Spec 003 created; all 10 acceptance criteria checked; status COMPLETE
- Specs 001, 002, 003 collectively describe the v1.0 → v1.1 → v1.2 evolution
- All three specs included in the upcoming commit
- Status: PASSED

### Files Added
- `tmux_lookup.py` — PWD → session lookup helper (installed as `vscl-tmux-lookup`)
- `specs/003-pwd-based-tmux-lookup.md`
- `tests/test_unit_tmux_lookup.py` — 15 new tests

### Files Modified
- `vscode_launcher.py` — removed `build_launch_env` and env-var plumbing; bumped `__version__` to "1.2"
- `tmux_hook.zsh` — now calls `vscl-tmux-lookup`; checks `VSCODE_INJECTION` / `VSCODE_PID` / `TERM_PROGRAM`
- `install.sh` — symlinks `vscl-tmux-lookup`; replaces existing hook block in place; smart placement before tmux auto-attach on fresh installs
- `uninstall.sh` — removes the `vscl-tmux-lookup` symlink
- `README.md` — new "How It Works" tmux-switching section, v1.2 changelog entry
- `tests/test_unit_vscode_launcher.py` — removed env-var tests (no longer applicable)

### Overall
- All gates passed: YES
- Version: 1.1 → 1.2
