## Validation Report: Edit Safety and Identifier Validation
**Date**: 2026-08-15 22:40
**Spec**: specs/003-edit-safety.md
**Status**: PASSED

### Phase 3: Tests
- Test suite: `QT_QPA_PLATFORM=offscreen /usr/bin/python3 -m pytest tests/ -q`
- Results: 57 passed, 0 failed (1.62s); 20 new in tests/test_safety.py
- Manual end-to-end against the live save, GUI offscreen:
  - novel mistyped id `SniperRiffle` flagged, status turns amber, row marked red
  - removing it clears both the warning and the highlight
  - external write raises the stale banner and disables Save
  - "Keep mine" re-baselines and re-enables Save
  - real save left byte-identical; its mtime reset after the staleness test
- Status: ✓ PASSED

### Phase 4: Code Quality
- Dead code: none introduced; `APPID_DEFAULT` reinstated in derive.py now that
  gamedata.py consumes it
- Duplication: none; validation logic lives only in validate.py
- Encapsulation: vocabulary harvesting (gamedata) separated from policy
  (validate) separated from presentation (gui)
- Fixes during review: `mark_suspects` initially cleared the gray styling on
  container rows and contained a nonsensical expression (`x and False`) in one
  test; both corrected
- Linters: ruff (F,E9) clean; flake8 --max-line-length=100 clean
- Status: ✓ PASSED

### Phase 5: Security Review
- Dependencies: unchanged (PyQt6, cryptography, numpy from system Python)
- Secrets: none introduced
- New file reads: `global-metadata.dat` (already read for key derivation) and
  Steam's `UserGameStatsSchema_<appid>.bin`. Both parsed defensively - schema
  ids are extracted by NUL-splitting and ASCII-decoding with failures skipped,
  no KeyValues parser is trusted with untrusted input
- New subprocess use: `pgrep -x steam`, `pgrep -x Megabonk.x86_64` - fixed
  argument lists, no shell
- `/proc` scan reads only `exe` symlinks and tolerates permission errors
- Privacy note: the tool now warns that achievement edits propagate to the
  user's public Steam profile, which is a disclosure improvement
- Status: ✓ PASSED

### Phase 5.5: Release Safety
- Change type: additive to an existing sub-project; no behaviour removed
- Rollback plan: `git revert`; the editor's own backup mechanism is untouched
- Blast radius: strictly reduces risk - the main change blocks a write path that
  could previously destroy a play session
- Status: ✓ PASSED

### Overall
- All gates passed: YES
- Notes: All three defects addressed were observed in real use, not theorised.
  The stale-buffer path came within one button press of reverting a live play
  session. Validation deliberately checks only user additions: validating the
  whole file flags 12 legitimate game-written ids on an untouched save, which
  would train the user to ignore the warning.
