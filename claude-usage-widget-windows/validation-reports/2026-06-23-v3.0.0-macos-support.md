## Validation Report: v3.0.0 — macOS Support

**Date**: 2026-06-23
**Spec**: specs/004-macos-support.md
**Status**: PASSED

### Phase 3: Tests
- Command: `QT_QPA_PLATFORM=offscreen /usr/bin/python3 -m pytest -q`
- Results: **64 passing, 0 failing**
- New coverage this release:
  - `tests/test_platform_paths.py` — macOS/Windows/generic config + log path branches (R11)
  - `tests/test_module_imports.py` — every pure module imports cleanly (regression for the PEP 604 / Python 3.9 crash)
  - `tests/test_widget_font.py` — font-size scaling, title sizing, persistence (offscreen Qt)
  - `tests/test_oauth.py` — added Keychain read/save/dispatch tests
- Manual verification on macOS (live):
  - Bundled `.app` launches; `tray_created` + `gui_running` logged (previously crashed at import on Python 3.9)
  - Live usage fetched from Keychain credentials (`--no-gui` → 5h/7d percentages)
  - `macos_window_level_applied ok=True`; widget stays on top across app focus changes (user-confirmed)
- Status: PASSED

### Phase 4: Code Quality
- Platform branching centralized in `src/platform_support.py` (`IS_WINDOWS`/`IS_MACOS`/`IS_LINUX`); no scattered `sys.platform` checks
- `src/oauth.py` credential read/save split into `_*_file` / `_*_keychain` helpers behind platform dispatchers — testable and symmetric
- Font sizing extracted to `FloatingWidget._apply_font_size()` / `set_font_size()`; menu styling factored into `_MENU_STYLE` constant (was duplicated)
- Native macOS window tweak isolated in `_apply_macos_window_level()`, best-effort with full `try/except`, gated on the `cocoa` QPA platform
- Status: PASSED (no critical issues)

### Phase 5: Security Review
- Dependency CVE scan: `pip-audit` **not installed** on this host — CVE scan skipped (not auto-installed per project tool policy). Dependencies unchanged from v2.0.0 (PySide6, structlog).
- No hardcoded secrets in `src/` (grep for `sk-ant-*` / long string literals → none). OAuth client ID is public.
- Credentials are never logged (only event names like `oauth_backoff_reset_creds_changed`).
- Keychain access via `subprocess.run` with a fixed argument list (no `shell=True`, no string interpolation into a shell) — no command injection. Note: the credential JSON is passed as a `security add-generic-password -w` argument, briefly visible in the local process list; acceptable for a single-user desktop tool writing its own credentials.
- Network calls use `urllib` with 10s timeouts.
- Status: PASSED (CVE scan deferred — tool unavailable)

### Phase 5.5: Release Safety
- Change type: Additive cross-platform support + macOS-specific bug fixes. No Windows/Linux behavior change (all new paths guarded by `IS_MACOS` / `cocoa` platform).
- Rollback plan: `git revert` the commit. On a user's machine, run `./uninstall.sh` (unloads LaunchAgent, removes `.app`, prompts before config/log deletion).
- Build artifacts (`build/`, `dist/`, generated `.icns`) are gitignored; the `.app` is reproducible via `scripts/build_macos.sh`.
- Status: PASSED

### Overall
- All gates passed: YES (CVE scan deferred — `pip-audit` unavailable; dependencies unchanged)
- Notes: macOS bring-up surfaced three issues beyond the original spec (Keychain credentials, PEP 604 crash on framework Python 3.9, `Qt.Tool` stay-on-top), all fixed and documented in the spec's deviations section. The `.app` is unsigned; first launch requires the Gatekeeper right-click→Open bypass and a one-time Keychain access approval.
