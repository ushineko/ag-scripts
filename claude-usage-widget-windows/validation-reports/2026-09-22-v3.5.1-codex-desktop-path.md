# Validation Report — v3.5.1 Codex Desktop-Path Discovery

**Date**: 2026-09-22
**Spec**: `specs/013-codex-desktop-path.md`
**Issue**: [#17](https://github.com/ushineko/ag-scripts/issues/17)

## Result

The shared provider resolves Codex through `CODEX_PATH`, PATH, then common
per-user installation directories. The same absolute path is used to start
app-server.

## Validation

- Standalone suite: 209 passed.
- Restricted-PATH live smoke: resolved `~/miniforge3/bin/codex` and fetched the
  current primary allowance successfully.
- Ruff and `git diff --check`: clean.
- Mirrored provider modules: byte-identical.

## Security

The executable is never passed through a shell. Explicit and discovered paths
must resolve to an executable file. No credentials, dependencies, or agent
configuration changed.

## Release safety

Additive discovery fallback only. Existing PATH resolution retains priority;
`CODEX_PATH` provides an explicit override. Rollback is a normal commit revert.
