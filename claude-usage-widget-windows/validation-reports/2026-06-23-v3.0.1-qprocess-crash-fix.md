## Validation Report: v3.0.1 — QProcess async fetch (QThread crash fix)

**Date**: 2026-06-23
**Spec**: specs/005-qprocess-async-fetch.md
**Status**: PASSED

### Phase 3: Tests
- Command: `QT_QPA_PLATFORM=offscreen /usr/bin/python3 -m pytest -q`
- Results: **70 passing, 0 failing** (added `tests/test_fetcher.py`: command construction frozen-vs-source, JSON parsing)
- Diagnosis: macOS crash report `claude-usage-widget-2026-06-23-092427.ips` — `EXC_CRASH/SIGABRT`, faulting main thread `abort ← QMessageLogger::fatal ← QThread::~QThread ← deleteLater`. Confirms "QThread destroyed while running".
- Functional verification (macOS):
  - `--fetch-json` from source and from the **frozen** `.app` binary → valid JSON on stdout, logs on stderr
  - Bundled `.app` stress test: update interval set to 3s, ran ~90s = **33 QProcess fetch cycles**, process stayed alive, **no crash and no new crash report** (count stayed at 3, all pre-fix)
  - Restored default 30s interval afterward
- Status: PASSED

### Phase 4: Code Quality
- New `src/fetcher.py` isolates the async fetch (`UsageFetcher` + `fetch_command`); `result_ready` emitted once (guarded against `finished`/`errorOccurred` double-fire)
- `src/main.py` GUI path simplified: `fetcher is not None` busy-guard; `deleteLater()` on a plain QObject after the child process has finished (no thread-teardown race)
- `setup_logging(stream=...)` added so the `--fetch-json` child routes logs to stderr, keeping stdout pure JSON
- No `QThread` usage remains in `src/` (only explanatory comments)
- Status: PASSED

### Phase 5: Security Review
- Dependency CVE scan: `pip-audit` not installed — skipped (not auto-installed per tool policy); dependencies unchanged.
- `QProcess` is started with a fixed argument list (`sys.executable` + literal flags) — no shell, no user-controlled input; no `shell=True`/`eval`/`exec` in `src/`.
- The `--fetch-json` payload on stdout is usage data only; credentials are never printed or logged.
- Logs routed to stderr in fetch mode contain event names only (no token values).
- Status: PASSED (CVE scan deferred — tool unavailable)

### Phase 5.5: Release Safety
- Change type: Bug fix + internal architecture swap (QThread → QProcess). No user-facing behavior change beyond "no longer crashes". Cross-platform (frozen .app and run-from-source both handled by `fetch_command`).
- Rollback plan: `git revert` the commit.
- Status: PASSED

### Overall
- All gates passed: YES (CVE scan deferred — `pip-audit` unavailable)
- Notes: Root cause identified from the macOS crash report; fix follows the repo's QProcess + event-loop convention (vscode-launcher, vpn-toggle). Verified at scale on the frozen bundle.
