## Validation Report: v3.1.0 — Terminal (TUI) modes for helper panes

**Date**: 2026-06-27
**Spec**: specs/007-tui-helper-pane.md
**Status**: PASSED

### Phase 3: Tests
- Command: `/usr/bin/python3 -m pytest tests/ -q`
- Results: **106 passing, 0 failing** (was 81; +22 in `tests/test_tui.py`, +3 in `tests/test_logging_config.py`)
- New tests (`tests/test_tui.py`, rich-based renderer):
  - `build_line` (compact --line): content (5h/7d), color thresholds carried as rich styles (green/yellow/red), unknown utilization (`--`), reset countdown, model breakdown, zero buckets omitted, note/stale parenthetical
  - color emission: rendering `build_line` through a `Console` emits ANSI when color enabled (`\x1b[32m`) and suppresses it when disabled
  - `build_tui_view` (full-width --tui): renders the progress bar + stats + reset; the row stretches to fill the console width; the stale note replaces the reset; not-logged-in / error fall back to the compact line
  - error states: not-logged-in (`None`), error dict (`rate_limited`, `offline`)
  - width truncation (--line): 5h always survives; model dropped before 7d; collapse to core when very narrow; no truncation when width is None (asserted via `Text.cell_len`)
  - `run_tui` loop (with `rich.live.Live` patched out): backs off on HTTP 429 then resets to base on success; honors `retry_after`; returns 0 on Ctrl-C
- New tests (`tests/test_logging_config.py`, regression for the TUI log-interleaving bug):
  - `console=True` writes to the stream; `console=False` discards (devnull) with nothing on stdout/stderr and `print()` unaffected; `console=False` + `log_file` routes to the file, terminal clean
- Live verification:
  - `python -m src.main --line` piped → compact plain line on stdout (`Claude 5h 9% · 7d 3% · reset 3h 56m`), **0 bytes on stderr** (rich auto-suppresses color off a TTY); `--no-color`/`--debug` also clean
  - Forced-color render of `build_tui_view(live_data)` at width 160 → full-width green `rich` progress bar (`━`), colored percentages, dim reset floated to the far right
  - `'PySide6' in sys.modules` is **False** after exercising the rich render path (rich is pure-Python, no Qt)
- Status: PASSED

### Phase 4: Code Quality
- New module `src/tui.py` is Qt-free (imports only `rich` + `oauth` + `display`); the GUI/PySide6 code path is untouched.
- Rendering switched from hand-rolled ANSI/escape sequences to `rich` (per maintainer request): `build_line` returns a styled `rich.text.Text`; `rich.live.Live` (alternate screen) handles redraw + pane clear/restore. Removed the now-dead `usage_ansi`/`ANSI_*` from `display.py` and the literal `\x1b[...]` constants/`_term_width` from `tui.py` (no hardcoded terminal sequences remain).
- Reuse: model-breakdown logic mirrors `widget._render_usage`; color thresholds reuse the `display.usage_color` cutoffs via `_usage_style`. No duplicated threshold constants.
- `main.py` mode wrappers (`run_line_mode`/`run_tui_mode`) follow the existing `run_fetch_json`/`run_no_gui` pattern. Logging dispatch is mode-aware: `--tui`/`--line` use `setup_logging(console=False)`, `--fetch-json` keeps stderr, GUI keeps stdout.
- Status: PASSED

### Phase 5: Security Review
- `pip-audit` not installed — CVE scan skipped. **New dependency added: `rich>=13.0.0`** (pulls `markdown-it-py`, `mdurl`). rich is a widely-used, actively-maintained pure-Python library; `rich==15.0.0` installed for the pane's `/usr/bin/python3` via `pip install --user` (maintainer approved adding the dep). CVE scan of the new deps is deferred until `pip-audit` is available; recommend running it before release.
- No hardcoded secrets; no credential values logged (the new paths never touch tokens — they call `fetch_claude_usage()` which already handles credentials).
- Output safety: only numeric, app-controlled values are interpolated (percentages via `format_percentage`/`:.0f`, countdown via `get_time_until_reset` → `Xh Ym`); API strings are not echoed raw. Color emission is handled by `rich.Console`, gated on TTY / `NO_COLOR` / `--no-color`.
- No `shell=True` / `eval` / `exec`.
- Status: PASSED (CVE scan of new deps deferred — `pip-audit` unavailable)

### Phase 5.5: Release Safety
- Change type: additive CLI feature (`--tui`, `--line`, `--interval`, `--no-color`) + new Qt-free module. Adds a runtime dependency (`rich`) required only on the terminal-mode path. No changes to existing GUI behavior or config schema.
- Rollback: `git revert` the commit (no migrations, no persisted-state changes). The added `rich` requirement is inert for the GUI path.
- Status: PASSED

### Overall
- All gates passed: YES (CVE scan of new deps deferred — `pip-audit` unavailable; run before release)
- Notes: The live `--tui` loop was exercised deterministically (mocking `time.sleep` and patching `rich.live.Live`); `timeout` is absent on macOS. Visual confirmation in an actual tmux/herd pane is left to the user's environment.
