# Validation Report — v3.5.2 Multi-Provider TUI Alignment

**Date**: 2026-09-22
**Spec**: `specs/014-tui-provider-alignment.md`
**Issue**: [#21](https://github.com/ushineko/ag-scripts/issues/21)

## Result

Claude rate-limit, Claude credit, and Codex rows now render in one shared Rich
table. Labels and windows occupy separate columns; bars and stats begin at
identical positions; reset text remains right-aligned.

## Validation

- Standalone suite: 210 passed.
- Focused alignment test renders all three payload shapes at width 180 and
  asserts identical bar and stats start positions.
- Live cached render visually verified with the current two Claude accounts and
  Codex account.
- Ruff, compileall, and `git diff --check`: clean.

## Security

Presentation-only change. No credentials, network behavior, cache behavior,
dependencies, subprocesses, or agent configuration changed. The diff contains
no secrets.

## Release safety

`build_tui_view` retains its public behavior for a single row. Multi-row callers
receive a Rich table instead of a Group, both accepted by `Live.update` and
`Console.print`. Rollback is a normal commit revert.
