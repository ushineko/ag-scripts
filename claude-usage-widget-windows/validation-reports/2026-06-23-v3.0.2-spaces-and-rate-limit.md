## Validation Report: v3.0.2 — Spaces/fullscreen visibility + rate-limit handling

**Date**: 2026-06-23
**Spec**: specs/006-spaces-and-rate-limit.md
**Status**: PASSED

### Phase 3: Tests
- Command: `QT_QPA_PLATFORM=offscreen /usr/bin/python3 -m pytest -q`
- Results: **81 passing, 0 failing**
- New/updated tests:
  - `tests/test_oauth.py` — 429 → `rate_limited` with parsed `retry_after`; other HTTP errors still `api_error`; `_parse_retry_after` (int / missing / HTTP-date)
  - `tests/test_widget_lastgood.py` — success caches+renders; transient error keeps cached reading + staleness; error/None with no cache shows error/not-logged-in; None after success keeps cache
  - `tests/test_config.py` — default interval now 60
- Diagnosis (bug #2): controlled `--fetch-json` loop reproduced the failure — usage endpoint returns **HTTP 429** after enough requests (`claude_usage_api_error status=429`), which mapped to `(API error)`.
- Live verification:
  - Forced 429 (5s interval): log showed `poll_backoff next_interval_s=300` — backed off to the server's `Retry-After` instead of hammering; no `(API error)` flashes
  - Rebuilt `.app` starts clean, `macos_window_level_applied ok=True` (collectionBehavior call succeeds), process stays alive
- Status: PASSED

### Phase 4: Code Quality
- Bug #1: `collectionBehavior` (canJoinAllSpaces | fullScreenAuxiliary) added to the existing cocoa-gated `_apply_macos_window_level()`.
- Bug #2: 429 handling isolated in `oauth.py` (`_parse_retry_after`); backoff in the long-lived GUI (`main.py`), since each fetch is a fresh QProcess with no shared state — the in-fetch-layer backoff that peripheral-battery-monitor uses (it runs in-process via QThread) would not persist here.
- Last-known-good: adopted peripheral-battery-monitor's pattern — `update_usage` split into cache-on-success / render-cached-on-error, with `_render_usage`, `_staleness_suffix`, `_apply_stale_indicator`, and a 60s staleness timer; tray mirrors it with `_render(stale=...)`.
- Status: PASSED

### Phase 5: Security Review
- `pip-audit` not installed — CVE scan skipped (deps unchanged).
- No hardcoded secrets; no credential values logged. No `shell=True`/`eval`/`exec`. `Retry-After` parsed defensively (int only; HTTP-date → None).
- Status: PASSED (CVE scan deferred — tool unavailable)

### Phase 5.5: Release Safety
- Change type: macOS-only window behavior + cross-platform rate-limit/last-known-good handling. Additive; no breaking changes. Default interval 30→60 affects only fresh configs (existing configs keep their value; the dev machine's was set to 60).
- Rollback: `git revert` the commit.
- Status: PASSED

### Overall
- All gates passed: YES (CVE scan deferred — `pip-audit` unavailable)
- Notes: Reviewed against peripheral-battery-monitor (where oauth.py was ported from) per maintainer guidance; backoff timing and last-known-good behavior follow its proven pattern, adapted to the QProcess architecture. Bug #1's cross-Space/fullscreen visibility is verified at the API level (native call ok); final visual confirmation is on the user's multi-Space/fullscreen setup.
